# Tyan chassis fan control -- covers ALL GPUs (core+VRAM), CPU sockets, NVMe, system memory.
# PROFILE: max-performance | performance | balanced | max-quiet   (crit thresholds are in the script)
PROFILE=max-quiet
INTERVAL=4
